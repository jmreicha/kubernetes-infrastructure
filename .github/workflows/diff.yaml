---
name: Diff ArgoCD Manifests

on:
  pull_request:
    paths:
      - "infrastructure/**"
      - "monitoring/**"
      - "network/**"
      - "security/**"

permissions:
  contents: read
  pull-requests: write

jobs:
  diff:
    container:
      image: quay.io/puzzle/goff:latest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          path: source

      - name: Checkout Target PR
        uses: actions/checkout@v4
        with:
          path: target
          ref: ${{ github.event.pull_request.base.ref }}

      - name: Create k8s Kind Cluster
        uses: helm/kind-action@v1.8.0
        with:
          cluster_name: "testing-cluster"

      - name: Bootstrap ArgoCD
        run: |
          kubectl create namespace argocd
          kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
          # TODO: Use kubectl wait command for resources to be ready
          sleep 10

      - name: Generate ArgoCD Diffs for Preview
        run: |
          # Source
          # helm template mychart ./source/helm/mychart --output-dir /tmp/source/out
          goff argocd app "./infrastructure/reloader.yaml" --repoServer="localhost:8081" --output-dir=/tmp/source/

          # Destination
          # helm template mychart ./target/helm/mychart --output-dir /tmp/target/out
          goff argocd app "./infrastructure/reloader" --repoServer="localhost:8081" --output-dir=/tmp/target/

          # Diff
          # goff diff "/tmp/source" "/tmp/target" --title=Preview --output-dir .
          goff diff "/tmp/source" "/tmp/target" --output-dir .

      - name: comment PR
        uses: machine-learning-apps/pr-comment@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          path: diff.md

      # TODO: Automatically merge PR if no changes
